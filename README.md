# Mapping system

## Endpoint

To get the first 100 locations:

`GET /location`

### Query params

- max: maximum number of locations
- format: response format (csv, json)

**Example:**

`/location?max=1&format=csv`

